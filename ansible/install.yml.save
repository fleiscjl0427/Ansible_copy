---

- hosts: all 
  become: true 
  tasks: 
 
  - name: install httpd package 
    dnf:
      name: httpd 
      state: latest
  
  
  - name: Update package index
    dnf:
      update_cache: yes
    when:
